define([
    'backbone', 'BaseView'
], function (Backbone, BaseView) {
    var ProjectContainerView = BaseView.extend({
        initialize: function () {

        },
        render: function () {
            return this;
        }
    });
    return ProjectContainerView;
});